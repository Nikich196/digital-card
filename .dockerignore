node_modules
dist
.git
.github

# Все локальные переменные окружения, а не только .env: .env.local создаётся
# инструментом хостинга и содержит рабочий токен — в контекст сборки он попадать
# не должен.
.env*

.vercel
*.log
README.md
